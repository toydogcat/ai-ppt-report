// Preloaded Presentation: 微積分 11.4 — 比較測試法 (The Comparison Tests)
const DECK_CALCULUS_11_4_MD = String.raw`# 微積分 11.4：比較測試法
## 副標題：借力使力——透過與已知級數的比較來判定收斂性
本簡報聚焦於 Stewart《Calculus》11.4 節，介紹直接比較測試法與極限比較測試法，教您如何快速識別級數的斂散性。

---

## 11.4 導言：類比的思維
### 如果它長得像收斂級數...
* **核心想法**：許多級數雖然形式複雜，但在 $n$ 非常大時，其行為會趨近於某個簡單的級數（如 p-級數或幾何級數）。
* **策略**：找一個「長得很像」且「已知斂散性」的級數 $\{b_n\}$ 來作為參考標竿。
* **適用對象**：**正項級數** ($\sum a_n, a_n > 0$)。

---

## 一、直接比較測試法 (Direct Comparison Test)
### 大小關係決定命運
設 $\sum a_n$ 與 $\sum b_n$ 皆為正項級數：
1. **收斂判定**：
   若 $\sum b_n$ **收斂** 且 **$a_n \le b_n$** 對所有 $n$ 成立，則 $\sum a_n$ 也收斂。
2. **發散判定**：
   若 $\sum b_n$ **發散** 且 **$a_n \ge b_n$** 對所有 $n$ 成立，則 $\sum a_n$ 也發散。

> 💡 **口訣**：比收斂的小必收斂，比發散的大必發散。

---

## 二、極限比較測試法 (Limit Comparison Test)
### 當大小關係難以直接判定時
若直接比較法的不等式方向不對（例如比收斂的大一點點），則改用極限：
計算極限：$c = \lim_{n \to \infty} \frac{a_n}{b_n}$
* 若 **$0 < c < \infty$**（$c$ 為正的有限常數）：
  > **$\sum a_n$ 與 $\sum b_n$ 的斂散性完全相同（同生共死）。**

* **優勢**：只需保留分子與分母的最高次項來選取 $b_n$，不需處理複雜的不等式。

---

## 三、如何選取參考級數 ($b_n$)？
### 抓大放小的藝術
在處理分式形式的 $a_n$ 時，保留 $n$ 的最高次冪。
* **範例 1**：$a_n = \frac{1}{2^n + 1}$
  $\to$ $b_n = \frac{1}{2^n}$（收斂幾何級數）。
* **範例 2**：$a_n = \frac{n^2 + 1}{n^4 + n + 5}$
  $\to$ $b_n = \frac{n^2}{n^4} = \frac{1}{n^2}$（收斂 p-級數）。
* **範例 3**：$a_n = \frac{1}{\sqrt{n^2 - 1}}$
  $\to$ $b_n = \frac{1}{\sqrt{n^2}} = \frac{1}{n}$（發散調和級數）。

---

## 四、估計級數的和
### 誤差傳遞
若我們已知 $a_n \le b_n$，且使用 $\sum b_n$ 來證明收斂：
* **餘項關係**：$R_n(a) \le R_n(b)$。
* **意義**：如果我們能算出參考級數 $b_n$ 的誤差上限，那麼原級數的誤差必定更小。

---

## 💡 總結：解題工作流
1. **觀察**：當 $n$ 很大時，$a_n$ 長得像什麼？
2. **選取**：定下參考級數 $b_n$（通常是 p-級數）。
3. **測試**：
   * 不等式明顯 $\to$ **直接比較**。
   * 不等式反向或複雜 $\to$ **極限比較**。

比較測試法是級數判定中最靈活、也最常用的技巧！
`;
