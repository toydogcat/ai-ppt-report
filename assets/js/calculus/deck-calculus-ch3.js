// Preloaded Presentation: 微積分 第三章 — 微分的應用 (Applications of Differentiation)
const DECK_CALCULUS_CH3_MD = String.raw`# 微積分 第三章：微分的應用
## 副標題：從極值問題到圖形分析，探索微分在現實世界中的威力
基於 Stewart《Calculus》第三章編寫。深入探討極大極小值、均值定理、函數圖形與最佳化問題。

---

## 3.1 極大值與極小值 (Max/Min Values)
### 絕對極值 vs. 區域極值
* **絕對極值 (Absolute Extrema)**：函數在整個定義域 $D$ 上的最大或最小值。
  * $f(c)$ 是絕對最大值，若 $\forall x \in D, f(c) \geq f(x)$。
* **區域極值 (Local Extrema)**：函數在某個包含 $c$ 的開區間內的極值。
  * $f(c)$ 是區域最大值，若在 $c$ 附近 $f(c) \geq f(x)$。
* **臨界數 (Critical Number)**：使 $f'(c) = 0$ 或 $f'(c)$ 不存在的點 $c$。

---

## 3.1 極值定理與費馬定理
### 尋找極值的關鍵
* **極值定理 (Extreme Value Theorem)**：若 $f$ 在閉區間 $[a, b]$ 連續，則必存在絕對最大值與絕對最小值。
* **費馬定理 (Fermat's Theorem)**：若 $f$ 在 $c$ 有區域極值，且 $f'(c)$ 存在，則 $boxed{f'(c) = 0}$。
* **閉區間法 (Closed Interval Method)**：
  1. 找出 $f$ 在 $(a, b)$ 內的所有臨界數。
  2. 計算端點 $f(a), f(b)$ 與臨界點的函數值。
  3. 最大者為絕對最大值，最小者為絕對最小值。

---

## 3.2 均值定理 (Mean Value Theorem)
### 導數與變化率的橋樑
* **羅爾定理 (Rolle's Theorem)**：若 $f$ 在 $[a, b]$ 連續、$(a, b)$ 可微，且 $f(a) = f(b)$，則至少存在一點 $c \in (a, b)$ 使得 $boxed{f'(c) = 0}$。
* **均值定理 (MVT)**：至少存在一點 $c \in (a, b)$ 使得切線斜率等於割線斜率：
  $$\boxed{f'(c) = \frac{f(b) - f(a)}{b - a}}$$
* **物理意義**：若平均速度為 $V$，則旅途中必有某一時刻的瞬時速度正好等於 $V$。

---

## 3.3 導數如何影響圖形形狀
### 一階與二階導數的檢測
* **增減函數測試**：
  * $f'(x) > 0 \implies f$ 在區間內遞增。
  * $f'(x) < 0 \implies f$ 在區間內遞減。
* **一階導數檢測法 (First Derivative Test)**：
  * $f'$ 從正變負 $\implies$ 區域最大值。
  * $f'$ 從負變正 $\implies$ 區域最小值。
* **凹凸性測試 (Concavity Test)**：
  * $f''(x) > 0 \implies$ 上凹 (Concave Up)。
  * $f''(x) < 0 \implies$ 下凹 (Concave Down)。
* **反曲點 (Inflection Point)**：圖形改變凹凸性的點。

---

## 3.4 二階導數檢測法
### 另一種判斷極值的方法
設 $f''(c)$ 連續且 $f'(c) = 0$：
1. 若 $f''(c) > 0$，則 $f$ 在 $c$ 有 **區域最小值**。
2. 若 $f''(c) < 0$，則 $f$ 在 $c$ 有 **區域最大值**。
3. 若 $f''(c) = 0$，則檢測法無效（需回到一階檢測法）。

> 💡 **小撇步**：$f''(c) > 0$ 代表上凹（像個碗），所以底端是最小值。

---

## 3.5 無窮遠處的極限與水平漸近線
### 探討函數的遠期行為
* **水平漸近線 (Horizontal Asymptote)**：
  若 $\lim_{x \to \infty} f(x) = L$ 或 $\lim_{x \to -\infty} f(x) = L$，則直線 $y = L$ 為水平漸近線。
* **計算技巧**：
  * 同除以分母的最高次方項。
  * $\lim_{x \to \infty} \frac{1}{x^n} = 0 \quad (n > 0)$。
* **鉛直漸近線 (Vertical Asymptote)**：
  若 $\lim_{x \to a} f(x) = \pm\infty$，則直線 $x = a$ 為鉛直漸近線。

---

## 3.7 最佳化問題 (Optimization Problems)
### 在現實生活中尋找「最好」的方案
1. **理解問題**：判斷哪些是已知，哪些是未知量。
2. **建立模型**：列出需要最佳化的目標函數。
3. **限制條件**：利用已知關係將變數減少至一個。
4. **求極值**：利用閉區間法或導數檢測法。

**範例**：如何設計一個容量固定且表面積最小的圓柱體罐頭？
* 關鍵在於建立 $A = 2\pi r^2 + 2\pi rh$ 與 $V = \pi r^2h$ 的關係。

---

## 3.9 反導函數 (Antiderivatives)
### 微分的逆運算
* **定義**：若 $F'(x) = f(x)$，則稱 $F$ 為 $f$ 的一個反導函數。
* **一般式**：$F(x) + C$，其中 $C$ 為任意常數（積分常數）。
* **基本公式**：
  * $\int x^n \, dx = \frac{x^{n+1}}{n+1} + C \quad (n \neq -1)$
  * $\int \cos x \, dx = \sin x + C$
  * $\int \sin x \, dx = -\cos x + C$
* **應用**：由加速度 $a(t)$ 求速度 $v(t)$，再求位移 $s(t)$。

---

## 本章總結
### 微分應用的核心邏輯
1. **極值判斷**：利用臨界點與 $f'$、$f''$。
2. **圖形分析**：結合漸近線、增減性與凹凸性。
3. **實務應用**：建立模型並求解最佳化問題。
4. **逆向思考**：反導函數為下一章「積分」鋪路。

微分不只是公式，它是描述自然界與經濟行為「變化」與「最適化」的最強武器。
`;
