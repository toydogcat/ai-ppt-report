// Preloaded Presentation: 微積分 3.1 — 極大值與極小值 (Maximum and Minimum Values)
const DECK_CALCULUS_3_1_MD = String.raw`# 微積分 3.1：極大值與極小值
## 副標題：尋找最佳化問題的解答，探索函數的最值
本簡報聚焦於 Stewart《Calculus》3.1 節，深入探討絕對極值與區域極值的定義、極值定理 (EVT) 以及如何利用導數尋找臨界數。

---

## 3.1 導言：為什麼尋找極值？
### 最佳化的藝術 (Optimization)
在現實世界中，我們經常需要找到「最好」或「最省」的方案：
* **企業**：如何最小化成本、最大化利潤？
* **工程**：如何設計形狀以最小化材料浪費？
* **科學**：如何找到火箭的最大加速度？
這些問題在數學上，都可以轉化為尋找一個函數的 **極大值** 或 **極小值**。

---

## 絕對極值 (Absolute Extrema)
### 全域的最高與最低點
設 $c$ 是函數 $f$ 定義域 $D$ 內的一個數：
* **絕對最大值 (Absolute Maximum)**：若 $\forall x \in D, f(c) \geq f(x)$，則 $f(c)$ 為絕對最大值。
* **絕對最小值 (Absolute Minimum)**：若 $\forall x \in D, f(c) \leq f(x)$，則 $f(c)$ 為絕對最小值。

> 💡 **注意**：一個函數在特定範圍內，可能只有最大值而無最小值，或者兩者皆無。

---

## 區域極值 (Local Extrema)
### 附近的頂峰與低谷
* **區域最大值 (Local Maximum)**：若當 $x$ 在 $c$ 附近時，$f(c) \geq f(x)$。
* **區域最小值 (Local Minimum)**：若當 $x$ 在 $c$ 附近時，$f(c) \leq f(x)$。

*形象理解：絕對極值是整座山脈的最高峰；區域極值則是某座山頭的頂端。*

---

## 極值定理 (Extreme Value Theorem, EVT)
### 什麼時候保證有最值？
> **定理內容**：若 $f$ 在 **閉區間 $[a, b]$** 上是 **連續的**，則 $f$ 必定在該區間內取得絕對最大值與絕對最小值。

**關鍵條件**：
1. **連續性**：函數圖形不能中斷。
2. **閉區間**：必須包含端點。
*缺少任一條件，最值都不保證存在。*

---

## 費馬定理 (Fermat's Theorem)
### 極值與導數的關係
> **定理內容**：若 $f$ 在 $c$ 點有區域極值，且 $f'(c)$ 存在，則：
$$\boxed{f'(c) = 0}$$

**重要警告**：
* $f'(c) = 0$ 並不代表該點一定是極值（例如 $y=x^3$ 在 $x=0$）。
* 極值也可能發生在導數不存在的地方（例如 $y=|x|$ 在 $x=0$）。

---

## 臨界數 (Critical Number)
### 極值的候選名單
**定義**：定義域內的點 $c$，若滿足以下任一條件，則稱為臨界數：
1. $f'(c) = 0$
2. $f'(c)$ 不存在

*結論：如果一個函數有區域極值，它必定發生在臨界數上。*

---

## 閉區間法 (The Closed Interval Method)
### 尋找絕對最值的標準流程
要在連續函數 $f$ 的閉區間 $[a, b]$ 上找到絕對極值：
1. 找出 $f$ 在開區間 $(a, b)$ 內的所有 **臨界數**。
2. 計算這些臨界數的 **函數值**。
3. 計算區間 **端點** 的函數值 $f(a)$ 與 $f(b)$。
4. **比較**：最大的值即為絕對最大值，最小的值即為絕對最小值。

---

## 💡 總結
* **定義**：區分「絕對」與「區域」的概念。
* **定理**：EVT 保證了最值的存在性。
* **工具**：費馬定理將幾何上的「頂點」轉化為代數上的「導數等於零」。
* **實踐**：透過臨界數與端點的比較，我們能系統性地解決所有連續函數在閉區間上的最值問題。
`;
