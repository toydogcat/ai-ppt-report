// Preloaded Presentation: 微積分 14.7 — 極大值與極小值 (Maximum and Minimum Values)
const DECK_CALCULUS_14_7_MD = String.raw`# 微積分 14.7：極大值與極小值
## 副標題：尋找曲面的巔峰與谷底——掌握多變數函數的最佳化
本簡報聚焦於 Stewart《Calculus》14.7 節，介紹如何定位局部極值、判別鞍點，以及在封閉區域尋找絕對極值。

---

## 14.7 導言：最佳化的目標
### 效率、利潤與穩定性
*   **目標**：在多變數環境下找到函數的最高點（最大值）或最低點（最小值）。
*   **定義**：
    *   **局部極大值**：在鄰域內 $f(a, b) \ge f(x, y)$。
    *   **局部極小值**：在鄰域內 $f(a, b) \le f(x, y)$。

---

## 一、臨界點 (Critical Points)
### 潛伏極值的地方
點 $(a, b)$ 稱為臨界點，若：
1.  $f_x(a, b) = 0$ 且 $f_y(a, b) = 0$（即梯度向量 $\nabla f = 0$）。
2.  或者其中一個偏導數不存在。
*   **幾何意義**：切平面在該點是**水平**的。
*   **警告**：臨界點不一定是極值點，它也可能是**鞍點 (Saddle Point)**。

---

## 二、二階導數測試 (Second Derivatives Test)
### 如何區分山峰、谷底與馬鞍？
設 $(a, b)$ 為 $f$ 的臨界點，定義判別式 $D$：
$$D = D(a, b) = f_{xx}(a, b) f_{yy}(a, b) - [f_{xy}(a, b)]^2$$
1.  **若 $D > 0$ 且 $f_{xx} > 0$** $\rightarrow$ **局部極小值**（凹向上）。
2.  **若 $D > 0$ 且 $f_{xx} < 0$** $\rightarrow$ **局部極大值**（凹向下）。
3.  **若 $D < 0$** $\rightarrow$ **鞍點**（一個方向升，一個方向降）。
4.  **若 $D = 0$** $\rightarrow$ 測試失效（不確定）。

---

## 三、絕對極值 (Absolute Extrema)
### 在封閉邊界內的極致
在一維中有極值定理，在二維中，若 $f$ 在**閉區域 $D$** 上連續，則必有絕對最大與最小值。
*   **尋找步驟**：
    1.  找出 $D$ 內部所有的臨界點及其函數值。
    2.  找出 $D$ **邊界 (Boundary)** 上的極值。
    3.  比較上述所有值，最大者為絕對最大值，最小者為絕對最小值。

---

## 四、實戰範例：紙盒的最佳化
### 體積固定，表面積最小
*   **問題**：設計一個無蓋長方體盒，體積固定為 $V = 32,000 \, \text{cm}^3$。如何選擇長 $x$、寬 $y$、高 $z$ 使材料最省（表面積最小）？
*   **函數**：$S(x, y) = xy + 2xz + 2yz$。
*   **方法**：代入 $z = V/(xy)$，求 $S(x, y)$ 的臨界點並用二階測試分類。

---

## 💡 總結：最佳化的邏輯路徑
1.  **求導**：算 $f_x, f_y$。
2.  **解方程**：令偏導為 0，找臨界點。
3.  **判別**：算 $D$ 與 $f_{xx}$。
4.  **邊界**：別忘了檢查區域的邊緣。

掌握了 14.7 節，您就擁有了在工程、經濟與資料科學中進行「最優選」的決策能力！
`;
