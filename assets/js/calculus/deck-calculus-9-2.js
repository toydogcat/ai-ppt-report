// Preloaded Presentation: 微積分 9.2 — 方向場與 Euler 方法 (Direction Fields and Euler's Method)
const DECK_CALCULUS_9_2_MD = String.raw`# 微積分 9.2：方向場與 Euler 方法
## 副標題：當無法求出精確解時，如何透過幾何與數值方法洞察函數行為
本簡報聚焦於 Stewart《Calculus》9.2 節，介紹如何利用方向場（幾何視覺化）與 Euler 方法（數值近似）來理解微分方程的解。

---

## 9.2 導言：如果解不出來怎麼辦？
### 視覺與數值的雙重進擊
* **現實困境**：大多數微分方程無法求得顯式的初等函數解。
* **策略一：方向場 (Direction Fields)**
  * **幾何法**：不解方程，直接畫出解曲線的「切線斜率」。
* **策略二：Euler 方法 (Euler's Method)**
  * **數值法**：利用切線不斷「步進」來逼近真實值。

---

## 一、方向場 (Direction Fields / Slope Fields)
### 預見解曲線的走向
對於微分方程 $y' = F(x, y)$：
* 在平面上的每個點 $(x, y)$，畫一段斜率為 $y'$ 的短線段。
* **解曲線 (Solution Curves)**：就像跟隨地圖上的箭頭前行，解曲線必須與這些短線段相切。
* **平衡解 (Equilibrium Solutions)**：在方向場中表現為水平的斜率線（$y' = 0$）。

---

## 二、Euler 方法：數值近似的基石
### 沿著切線走一小步
對於初值問題 $y' = F(x, y), y(x_0) = y_0$：
* **核心想法**：從已知點出發，沿著當前斜率前進一小步 $h$。
* **疊代公式**：
  $$\boxed{y_n = y_{n-1} + h \cdot F(x_{n-1}, y_{n-1})}$$
* **參數 $h$ (Step Size)**：步長越小，近似越精確，但計算量也越大。

---

## 範例：Euler 方法實戰
### 求解 $y' = x + y, y(0) = 1$，步長 $h = 0.1$
1. **第一步** ($n=1$):
   $x_0 = 0, y_0 = 1, F(0, 1) = 0 + 1 = 1$
   $y_1 = 1 + 0.1(1) = 1.1$
2. **第二步** ($n=2$):
   $x_1 = 0.1, y_1 = 1.1, F(0.1, 1.1) = 0.1 + 1.1 = 1.2$
   $y_2 = 1.1 + 0.1(1.2) = 1.22$
3. **結論**：$y(0.2) \approx 1.22$。

---

## 💡 總結：工具的選擇
* **方向場**：提供「全局視野」。適合觀察解的長期趨勢（如：是否會發散或趨向穩定值）。
* **Euler 方法**：提供「局部數值」。適合在特定點給出具體的近似估計。

微分方程的圖形法與數值法，是現代科學計算（如氣象預測、導彈彈道分析）的底層邏輯。
`;
