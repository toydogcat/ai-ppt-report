// Preloaded Presentation: 微積分 14.6 — 方向導數與梯度向量 (Directional Derivatives and the Gradient Vector)
const DECK_CALCULUS_14_6_MD = String.raw`# 微積分 14.6：方向導數與梯度向量
## 副標題：全方位變動率——掌握曲面上「最陡」的方向與指針
本簡報聚焦於 Stewart《Calculus》14.6 節，介紹如何計算任意方向的斜率、梯度向量的定義及其強大的幾何特性。

---

## 14.6 導言：為什麼需要方向導數？
### 突破座標軸的限制
*   **偏導數 $f_x, f_y$**：只能告訴我們沿著 $x$ 軸或 $y$ 軸正方向的斜率。
*   **方向導數**：讓我們能計算沿著 $xy$ 平面上**任意方向**單位向量 $u = \langle a, b \rangle$ 的變動率。
*   **符號**：$D_u f(x, y)$。

---

## 一、方向導數的計算公式
### 利用偏導數的組合
若 $f$ 是可微函數，$u = \langle \cos \theta, \sin \theta \rangle$ 是單位向量，則：
$$D_u f(x, y) = f_x(x, y) \cos \theta + f_y(x, y) \sin \theta$$
*   **重要前提**：$u$ 必須是**單位向量**（長度等於 1）。
*   **幾何直覺**：這是 $x$ 與 $y$ 方向斜率的「加權平均」。

---

## 二、梯度向量 (The Gradient Vector)
### 多變數微分的核心指針
定義 $f$ 的梯度向量 $\nabla f$ (讀作 "del f") 為：
$$\nabla f(x, y) = \langle f_x(x, y), f_y(x, y) \rangle = \frac{\partial f}{\partial x} i + \frac{\partial f}{\partial y} j$$
*   **方向導數的向量形式**：
    $$D_u f(x, y) = \nabla f(x, y) \cdot u$$
    （方向導數等於梯度向量與方向向量的**內積**！）

---

## 三、梯度的幾何特性 (1)：最大變動率
### 哪裡最陡？
根據內積性質 $\nabla f \cdot u = |\nabla f||u| \cos \phi$：
1.  **最大增加率**：當 $u$ 與 $\nabla f$ **同向**時，增加最快。最大值為 $|\nabla f|$。
2.  **最大減少率**：當 $u$ 與 $\nabla f$ **反向**時，減少最快。最小值為 $-|\nabla f|$。
3.  **零變動率**：當 $u$ 與 $\nabla f$ **垂直**時，變化率為 0。
> **結論**：梯度向量指向函數**增加最快**的方向。

---

## 四、梯度的幾何特性 (2)：垂直性
### 正交於等高線與等位面
*   **等高線**：在點 $(x_0, y_0)$，梯度向量 $\nabla f(x_0, y_0)$ 必垂直於通過該點的等高線 $f(x, y) = k$。
*   **切平面應用**：對於三元函數 $F(x, y, z) = k$，曲面上點 $P$ 的切平面法向量即為 $\nabla F(P)$。
    *   **切平面方程式**：
        $$F_x(P)(x-x_0) + F_y(P)(y-y_0) + F_z(P)(z-z_0) = 0$$

---

## 💡 總結：多變數微積分的指南針
*   **方向導數**：告訴我們「往這走斜率是多少」。
*   **梯度向量**：告訴我們「往哪走最陡」以及「這點的法線在哪」。
*   **三位一體**：梯度、方向導數、法向量三者緊密相連。

掌握了 14.6 節，您就擁有了在複雜曲面上尋路、導航與建模的終極工具！
`;
