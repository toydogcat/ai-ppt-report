// Preloaded Presentation: 微積分 12.5 — 空間中的直線與平面 (Equations of Lines and Planes)
const DECK_CALCULUS_12_5_MD = String.raw`# 微積分 12.5：空間中的直線與平面
## 副標題：代數與幾何的交會——精確描述三維空間中的基礎結構
本簡報聚焦於 Stewart《Calculus》12.5 節，介紹直線與平面的各種方程式形式、相互關係以及距離計算。

---

## 一、空間中的直線 (Lines in Space)
### 定義：一個點 + 一個方向
一條直線 $L$ 由點 $P_0(x_0, y_0, z_0)$ 與平行於線的**方向向量** $v = \langle a, b, c \rangle$ 決定。
1. **向量方程式**：$r = r_0 + tv$
2. **參數方程式 (Parametric Equations)**：
   $$x = x_0 + at, \quad y = y_0 + bt, \quad z = z_0 + ct$$
3. **對稱方程式 (Symmetric Equations)**：
   $$\frac{x-x_0}{a} = \frac{y-y_0}{b} = \frac{z-z_0}{c}$$

---

## 二、線段與特殊線關係
### 從無限直線到有限區間
* **線段 (Line Segments)**：從 $r_0$ 到 $r_1$ 的線段表示為：
  $$r(t) = (1-t)r_0 + tr_1, \quad 0 \le t \le 1$$
* **歪斜線 (Skew Lines)**：
  在空間中，既不平行也不相交的兩條直線稱為歪斜線。它們位於不同的平行平面上。

---

## 三、空間中的平面 (Planes in Space)
### 定義：一個點 + 一個垂直方向
一個平面由點 $P_0(x_0, y_0, z_0)$ 與垂直於平面的**法向量 (Normal Vector)** $n = \langle a, b, c \rangle$ 決定。
1. **標量方程式 (Scalar Equation)**：
   $$a(x-x_0) + b(y-y_0) + c(z-z_0) = 0$$
2. **一般線性方程式 (General Equation)**：
   $$ax + by + cz + d = 0$$
   * **技巧**：通過三點的平面？先找兩個平面上的向量，取其外積作為法向量。

---

## 四、交集與夾角
### 幾何物件的互動
* **線與平面的交點**：將直線的參數式代入平面方程式求 $t$。
* **兩平面的夾角**：定義為其**法向量**之間的銳夾角 $\theta$。
  $$\cos \theta = \frac{|n_1 \cdot n_2|}{|n_1||n_2|}$$
* **交線**：兩個不平行的平面相交於一條直線。交線的方向向量 $v = n_1 \times n_2$。

---

## 五、距離公式 (Distances)
### 點到平面的最短路徑
點 $P_1(x_1, y_1, z_1)$ 到平面 $ax + by + cz + d = 0$ 的距離 $D$ 為：
$$D = \frac{|ax_1 + by_1 + cz_1 + d|}{\sqrt{a^2 + b^2 + c^2}}$$
* **原理**：這實際上是連線向量在法向量上的投影長度。
* **應用**：計算兩平行平面間的距離（取一面上的一點計算到另一面的距離）。

---

## 💡 總結：空間幾何的建模核心
* **描述方式**：
  * **直線**：用「平行」的方向向量描繪。
  * **平面**：用「垂直」的法向量定位。
* **核心工具**：
  * **內積**：用於計算夾角與距離（投影）。
  * **外積**：用於生成垂直的方向（如平面的法向量）。

掌握了線與面的方程，您就擁有了建構與分析三維複雜形狀的基本能力！
`;
