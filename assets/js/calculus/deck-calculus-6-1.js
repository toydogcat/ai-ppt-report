// Preloaded Presentation: 微積分 6.1 — 反函數 (Inverse Functions)
const DECK_CALCULUS_6_1_MD = String.raw`# 微積分 6.1：反函數及其導數
## 副標題：逆轉函數的運作，探索對稱性與變化的互惠關係
本章節我們將放慢腳步，深入探討 Stewart《Calculus》6.1 節。從一對一函數的嚴謹定義出發，逐步推導出反函數的性質、求法，以及最重要的：如何計算反函數的導數。

---

## 6.1 導言：一對一函數 (One-to-One Functions)
### 擁有反函數的先決條件
並非所有函數都能「倒帶」。要能從輸出完美推回唯一的輸入，函數必須是「一對一」的。
*   **嚴謹定義**：函數 $f$ 從不重複取得相同的輸出值。
    > 若 $x_1 \neq x_2$，則 $f(x_1) \neq f(x_2)$。
*   **水平線測試 (Horizontal Line Test)**：
    > 一個非常視覺化的檢查法：如果畫一條水平線，與函數的圖形 (graph) 相交**不超過 1 個點**，那它就是一對一函數。
*   **範例對比**：
    *   $f(x) = x^3$ 是一對一的（通過測試）。
    *   $f(x) = x^2$ 不是一對一的（一條水平線可能交於兩點，例如 $f(1)=f(-1)=1$）。

---

## 一、反函數的正式定義
### 映射的完美逆轉
只有當函數 $f$ 是一對一函數時，我們才能定義它的**反函數 (Inverse Function)**，記作 $f^{-1}$。
*   設 $f$ 的定義域為 $A$，值域為 $B$。
*   則 $f^{-1}$ 的定義域為 $B$，值域為 $A$，且滿足：
    $$\boxed{f^{-1}(y) = x \iff f(x) = y}$$

**消除性質 (Cancellation Equations)**：
這兩個函數合成後會互相抵消，回到起點（Identity function, $id(x)$）：
1.  $f^{-1}(f(x)) = x$ 對所有 $x \in A$ 成立。
2.  $f(f^{-1}(x)) = x$ 對所有 $x \in B$ 成立。

---

## 二、如何找出反函數？
### 代數操作的三個步驟
如果我們需要求出反函數 $f^{-1}$ 的確切公式，可以遵循以下標準步驟：
1.  **寫出方程式**：將函數寫成 $y = f(x)$ 的形式。
2.  **解出 $x$**：透過代數運算，將 $x$ 表達為 $y$ 的函數。這一步有時可能很困難甚至無法做到解析解。
3.  **互換變數**：為了習慣上以 $x$ 作為自變數，將方程式中的 $x$ 與 $y$ 互換。此時的 $y$ 就是我們要求的 $f^{-1}(x)$。

*   **實戰範例 (參考 P415)**：嘗試對 $f(x) = \frac{x^5-3}{2}$ 進行上述步驟，您會發現 $f^{-1}(x) = \sqrt[5]{2x+3}$。

---

## 三、反函數的幾何圖形
### 鏡像的藝術
*   **幾何特徵**：從視覺上來看，$f^{-1}$ 的圖形是 $f$ 的圖形對著直線 **$y = x$** 進行鏡射 (Reflection) 所得到的結果。
*   **點的對換**：這在代數上的意義是，若點 $(a, b)$ 位於 $f$ 的圖形上，那麼點 $(b, a)$ 必然位於 $f^{-1}$ 的圖形上。
*   **繪圖技巧**：如果您知道原函數的長相，只需沿著 45 度角的對角線翻轉，就能畫出其反函數。

---

## 四、反函數的導數 (1)：重要公式
### 變化的互惠關係
我們最關心的是：如果 $f$ 是可微的，那麼 $f^{-1}$ 可微嗎？它的導數該怎麼算？
> 假設 $f$ 是可微的一對一函數，且其反函數為 $f^{-1}$。
> 若 $f'(f^{-1}(a)) \neq 0$，則 $f^{-1}$ 在 $a$ 點的導數為：
> $$\boxed{(f^{-1})'(a) = \frac{1}{f'(f^{-1}(a))}}$$

**直觀理解**：
如果原函數在某點很「陡」（$f'$ 很大），翻轉到 $y=x$ 的另一側後，反函數在那點就會變得很「平緩」（$(f^{-1})'$ 很小），兩者互為倒數。

---

## 五、反函數的導數 (2)：公式推導
### 隱微分與連鎖律的完美結合
我們可以用隱函數微分 (Implicit Differentiation) 的觀點來推導這個重要公式：
1.  利用消除性質構造恆等式：
    $$f \circ f^{-1} (x) = x$$
    （即 $f(f^{-1}(x)) = x$）
2.  對等式兩邊同時對 $x$ 微分，並應用**連鎖律 (Chain Rule)**：
    $$f' \Big(f^{-1}(x)\Big) \cdot (f^{-1})'(x) = 1$$
3.  將已知項移項，即可得到公式：
    $$(f^{-1})'(x) = \frac{1}{f' \Big(f^{-1}(x)\Big)}$$

---

## 六、實戰演練：計算反函數導數
### 不必解出反函數也能求導數！
**範例**：若 $f(x) = 2x + \cos x$，求 $(f^{-1})'(1)$
1.  **找出對應點**：我們需要知道 $f$ 的哪個輸入會得到輸出 1。
    觀察得知 $f(0) = 2(0) + \cos 0 = 0 + 1 = 1$。
    因此，我們確定了 $f^{-1}(1) = 0$。
2.  **求原函數導數**：$f'(x) = 2 - \sin x$。
3.  **帶入公式**：
    $$(f^{-1})'(1) = \frac{1}{f'(f^{-1}(1))} = \frac{1}{f'(0)}$$
    $$= \frac{1}{2 - \sin 0} = \frac{1}{2 - 0} = \boxed{\frac{1}{2}}$$

---

## 💡 總結
*   **一對一性**：是函數擁有反函數的保證，請善用「水平線測試」。
*   **代數與幾何**：反函數是變數的互換，也是圖形對 $y=x$ 的完美鏡射。
*   **微積分核心**：連鎖律推導出的導數公式 $(f^{-1})' = 1/f'$，讓我們能優雅地繞過繁瑣的代數解法，直接從原函數獲取反函數的變動率。

掌握了反函數的這些細節，您就具備了處理指數與對數（下一節重點）以及反三角函數的強大數學基石！
`;
