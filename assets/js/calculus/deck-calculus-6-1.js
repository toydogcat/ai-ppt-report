// Preloaded Presentation: 微積分 6.1 — 反函數 (Inverse Functions)
const DECK_CALCULUS_6_1_MD = String.raw`# 微積分 6.1：反函數及其導數
## 副標題：逆轉函數的運作，探索對稱性與變化的互惠關係
本章節我們將放慢腳步，深入探討 Stewart《Calculus》6.1 節。從一對一函數的嚴謹定義出發，逐步推導出反函數的性質、求法，以及最重要的：如何計算反函數的導數。

---

## 一、一對一函數 (1/2)
### 擁有反函數的先決條件
並非所有函數都能「倒帶」。要能從輸出完美推回唯一的輸入，函數必須是「一對一」的。

*   **嚴謹定義**：函數 $f$ 從不重複取得相同的輸出值。
    > 若 $x_1 \neq x_2$，則 $f(x_1) \neq f(x_2)$。
*   如果函數不滿足一對一，反向對應時就會出現「一個輸出對應多個輸入」的困惑。

---

## 一、一對一函數 (2/2)
### 水平線測試 (Horizontal Line Test)
這是一個非常視覺化的檢查法：
*   **方法**：在圖形上畫一條水平線。
*   **判準**：如果任何水平線與函數圖形相交**不超過 1 個點**，那它就是一對一函數。

![水平線測試圖解](assets/img/calculus_6_1_horizontal_test.webp)

---

## 範例對比：一對一性
### 直觀判斷圖形
*   **$f(x) = x^3$**：
    *   是一對一的。
    *   任何高度都只對應一個 $x$ 值。
*   **$f(x) = x^2$**：
    *   **不是**一對一的。
    *   一條水平線可能交於兩點（例如 $f(1)=f(-1)=1$）。

---

## 二、反函數的正式定義
### 映射的完美逆轉
設 $f$ 的定義域為 $A$，值域為 $B$。
若 $f$ 是一對一函數，其**反函數 (Inverse Function)** $f^{-1}$ 滿足：

$$\boxed{f^{-1}(y) = x \iff f(x) = y}$$

*   $f^{-1}$ 的**定義域**是 $f$ 的**值域** ($B$)。
*   $f^{-1}$ 的**值域**是 $f$ 的**定義域** ($A$)。

---

## 三、消除性質 (Cancellation)
### 函數的「自我抵消」
反函數與原函數合成後會回到起點：

1.  **$f^{-1}(f(x)) = x$** 對所有 $x \in A$ 成立。
    > （先正向再反向，回到 $x$）
2.  **$f(f^{-1}(x)) = x$** 對所有 $x \in B$ 成立。
    > （先反向再正向，回到 $x$）

這在代數化簡與解方程式中極其有用。

---

## 四、如何找出反函數？
### 代數操作的三個步驟
1.  **寫出方程式**：將函數寫成 $y = f(x)$ 的形式。
2.  **解出 $x$**：透過代數運算，將 $x$ 表達為 $y$ 的函數。
    > （這一步決定了反函數的長相）
3.  **互換變數**：將方程式中的 $x$ 與 $y$ 互換。
    > 此時的 $y$ 即為 $f^{-1}(x)$。

---

## 實戰範例：找出反函數
### 範例：$f(x) = \frac{x^5-3}{2}$
1.  寫出：$y = \frac{x^5-3}{2}$
2.  移項：$2y = x^5 - 3 \implies 2y + 3 = x^5$
3.  解出 $x$：$x = \sqrt[5]{2y + 3}$
4.  互換 $x, y$：
    $$\boxed{f^{-1}(x) = \sqrt[5]{2x+3}}$$

---

## 五、反函數的幾何圖形
### 鏡像的藝術
*   **幾何特徵**：$f^{-1}$ 的圖形是 $f$ 的圖形對著直線 **$y = x$** 進行鏡射 (Reflection) 的結果。
*   **點的對換**：
    > 若點 $(a, b)$ 位於 $f$ 的圖形上，
    > 則點 $(b, a)$ 必然位於 $f^{-1}$ 的圖形上。
*   這就是為什麼定義域與值域會互換的幾何原因。

---

## 六、反函數的導數 (公式)
### 變化的互惠關係
假設 $f$ 是可微的一對一函數，若 $f'(f^{-1}(a)) \neq 0$，則：

$$\boxed{(f^{-1})'(a) = \frac{1}{f'(f^{-1}(a))}}$$

**物理直覺**：
> 如果 $y$ 增加的速度是 $x$ 的 2 倍，那麼在對等的位置上，$x$ 增加的速度就是 $y$ 的 $1/2$ 倍。

---

## 七、導數公式推導 (1/2)
### 利用隱微分法
我們可以證明這個看似複雜的公式：
1.  利用消除性質：
    $$f(f^{-1}(x)) = x$$
2.  對等式兩邊同時對 $x$ 微分。

---

## 七、導數公式推導 (2/2)
### 應用連鎖律 (Chain Rule)
3.  左邊微分：$f' (f^{-1}(x)) \cdot (f^{-1})'(x)$
4.  右邊微分：$1$
5.  得出等式：
    $$f' (f^{-1}(x)) \cdot (f^{-1})'(x) = 1$$
6.  移項即得公式：
    $$(f^{-1})'(x) = \frac{1}{f' (f^{-1}(x))}$$

---

## 八、實戰演練：不必解出 $f^{-1}$
### 範例：若 $f(x) = 2x + \cos x$，求 $(f^{-1})'(1)$
**第一步：找出對應點**
*   我們想求 $f^{-1}(1)$，即解 $f(x) = 1$。
*   觀察得知 $f(0) = 2(0) + \cos 0 = 1$。
*   所以 $\mathbf{f^{-1}(1) = 0}$。

---

## 八、實戰演練：帶入公式
### 範例：若 $f(x) = 2x + \cos x$，求 $(f^{-1})'(1)$
**第二步：求原函數導數**
*   $f'(x) = 2 - \sin x$
**第三步：代入 $x=0$ 並取倒數**
*   $(f^{-1})'(1) = \frac{1}{f'(f^{-1}(1))} = \frac{1}{f'(0)}$
*   $= \frac{1}{2 - \sin 0} = \frac{1}{2 - 0} = \boxed{\frac{1}{2}}$

---

## 💡 總結
*   **一對一性**：是存在的保證，請善用「水平線測試」。
*   **幾何特徵**：圖形關於 $y=x$ 對稱。
*   **微積分核心**：不必求出反函數的解析式，利用公式就能算出其導數。
*   **口訣**：反函數在某點的斜率，等於原函數在對應點斜率的**倒數**。

掌握了這些細節，您就具備了處理指數、對數與反三角函數的強大數學基石！
`;
