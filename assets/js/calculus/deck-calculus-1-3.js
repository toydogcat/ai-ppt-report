// Preloaded Presentation: 微積分 1.3 — 從舊函數建立新函數 (New Functions from Old Functions)
const DECK_CALCULUS_1_3_MD = String.raw`# 微積分 1.3：從舊函數建立新函數
## 副標題：掌握變換、組合與複合的藝術，建構複雜函數的積木
本簡報聚焦於 Stewart《Calculus》1.3 節，介紹如何透過平移、伸縮與反射來變換函數圖形，以及如何利用算術運算與「複合」來組合函數。

---

## 1.3 導言：函數的模組化
### 變化的多樣性
* **現狀**：我們有一套基本函數工具箱（線性、冪函數、三角等）。
* **目標**：現實世界中的函數往往是基本函數的變體。
* **核心技術**：
  1. **圖形變換**：移動或改變現有圖形的形狀。
  2. **函數組合**：將兩個函數加、減、乘、除。
  3. **函數複合**：將一個函數的輸出作為另一個函數的輸入。

---

## 圖形變換：平移與對稱
### 1. 平移 (Shifting)
若 $c > 0$：
* $y = f(x) + c$：向上平移 $c$ 單位。
* $y = f(x - c)$：向右平移 $c$ 單位。

### 2. 反射 (Reflecting)
* $y = -f(x)$：對 $x$ 軸反射（垂直翻轉）。
* $y = f(-x)$：對 $y$ 軸反射（水平翻轉）。

---

## 圖形變換：伸縮 (Stretching)
### 改變函數的「幅度」與「頻率」
若 $c > 1$：
* **垂直伸縮**：$y = cf(x)$ 垂直拉伸；$y = (1/c)f(x)$ 垂直壓縮。
* **水平伸縮**：$y = f(cx)$ 水平壓縮；$y = f(x/c)$ 水平拉伸。

> 💡 **小撇步**：對 $x$ 的變換通常與直覺「相反」（乘以 $c$ 反而是壓縮）。

---

## 函數的算術組合
### 基礎運算
給定兩個函數 $f$ 與 $g$：
* **和與差**：$(f \pm g)(x) = f(x) \pm g(x)$。
* **積與商**：$(fg)(x) = f(x)g(x)$；$(f/g)(x) = f(x)/g(x)$。
* **定義域**：新函數的定義域是 $f$ 與 $g$ 定義域的 **交集**。對於商函數，還需排除 $g(x) = 0$ 的點。

---

## 函數的複合 (Composition)
### 鏈條般的依賴關係
**定義**：將 $g(x)$ 代入 $f$ 中，記作 $(f \circ g)(x) = f(g(x))$。
* **處理順序**：先執行內層 $g$，再執行外層 $f$。
* **注意**：一般來說 $f \circ g \neq g \circ f$。順序至關重要！

**範例**：若 $f(x) = x^2, g(x) = x-3$
* $(f \circ g)(x) = (x-3)^2$
* $(g \circ f)(x) = x^2 - 3$

---

## 視覺化應用：日光時數模型
### 綜合變換的威力
在費城（北緯 40 度），日照時數可用正弦函數變換來描述：
$$L(t) = 12 + 2.8 \sin\left[\frac{2\pi}{365}(t-80)\right]$$
* **12**：垂直平移（平均時數）。
* **2.8**：垂直拉伸（變化振幅）。
* **2\pi/365**：水平伸縮（對應一年 365 天）。
* **80**：水平平移（從春分開始計算）。

---

## 💡 總結
* **變換** 讓我們能透過簡單公式預測圖形的樣貌。
* **組合與複合** 展現了數學語言的模組化能力，讓我們能從簡單零件組裝出複雜模型。
* **核心直覺**：學會區分「外層」與「內層」的操作，這對未來的「鏈鎖律 (Chain Rule)」微分至關重要。

掌握了這些變換技巧，任何函數在您眼中都不再是冰冷的符號，而是生動的幾何變形！
`;
