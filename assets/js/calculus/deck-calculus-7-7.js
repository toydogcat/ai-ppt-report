// Preloaded Presentation: 微積分 7.7 — 近似積分與誤差估計 (Approximate Integration and Error Estimates)
const DECK_CALCULUS_7_7_MD = String.raw`# 微積分 7.7：近似積分與誤差估計
## 副標題：當精確解不可得，如何利用數值方法逼近真相並控制誤差
本簡報聚焦於 Stewart《Calculus》7.7 節，介紹三種核心的數值積分方法：梯形法、中點法與辛普森法，並探討如何估計其與真實值的偏差。

---

## 7.7 導言：為什麼需要近似？
### 跨越「不可積」的障礙
有兩類情況我們無法求得精確積分：
1. **找不到反導函數**：例如 $\int e^{x^2} dx$ 或 $\int \frac{\sin x}{x} dx$。這類函數在初等代數中不可積。
2. **數據驅動型**：函數僅由實驗觀測點組成（如氣溫、車速記錄），並無解析式。
* **目標**：用有限的計算次數，得到足夠精確的數值結果。

---

## 策略一：梯形法則 (Trapezoidal Rule)
### 用直線段逼近曲線
將區間分成 $n$ 等分，每一段用 **梯形** 代替矩形：
$$\boxed{T_n = \frac{\Delta x}{2} [f(x_0) + 2f(x_1) + 2f(x_2) + \dots + f(x_n)]}$$
* **$\Delta x = (b-a)/n$**。
* **邏輯**：它是左端點法與右端點法的平均值。

---

## 策略二：中點法則 (Midpoint Rule)
### 區間中心的平衡點
使用每一小段區間的 **中心點高度** 來計算矩形面積：
$$\boxed{M_n = \Delta x [f(\bar{x}_1) + f(\bar{x}_2) + \dots + f(\bar{x}_n)]}$$
* **特性**：通常比梯形法則更精確，因為正負偏差更容易在內部抵消。

---

## 策略三：辛普森法則 (Simpson's Rule)
### 用拋物線捕捉彎曲
這是在對等分區間上最高效的近似方法。它利用 **拋物線弧** 來連接每三個點：
$$\boxed{S_n = \frac{\Delta x}{3} [f(x_0) + 4f(x_1) + 2f(x_2) + 4f(x_3) + \dots + f(x_n)]}$$
* **限制**：$n$ 必須為 **偶數**。
* **權重規律**：係數呈現 $1, 4, 2, 4, 2, \dots, 4, 1$ 的規律。

---

## 誤差估計 (Error Estimates)
### 我們離真相有多遠？
誤差定義為 $E = \text{真實值} - \text{近似值}$。
1. **梯形法誤差 $E_T$**：與 $f''(x)$ 最大值有關。
2. **中點法誤差 $E_M$**：約為梯形法誤差的一半，且符號相反。
3. **辛普森法誤差 $E_S$**：與 **四階導數 $f^{(4)}(x)$** 有關，收斂速度極快。
   * *這意味著如果 $f(x)$ 是三次以下多項式，辛普森法將得到 **完全正確** 的結果！*

---

## 💡 總結與應用建議
* **精度排序**：通常情況下，$S_n > M_n > T_n$。
* **實務操作**：
  1. 數據分佈均勻時優先使用 **Simpson's Rule**。
  2. 需要估計誤差時，先求出相關階數導數的上界。
* **科技輔助**：現代計算機程式（如 Python 的 \`scipy.integrate.simps\`）多採用此類算法。

掌握了近似積分，您就擁有了在現實科研中處理「不完美」數據並將其量化的科學工具！
`;
