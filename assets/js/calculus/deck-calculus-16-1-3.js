// Preloaded Presentation: 微積分 16.1 — 向量場 (Vector Fields)
const DECK_CALCULUS_16_1_MD = String.raw`# 微積分 16.1：向量場
## 副標題：描述空間中無處不在的力量——從風速到引力
本簡報聚焦於 Stewart《Calculus》16.1 節，介紹向量場的定義、視覺化方法以及重要的場類型。

---

## 16.1 導言：什麼是向量場？
### 點與向量的結合
*   **定義**：一個函數 $F$，它將區域中的每一點 $(x, y, z)$ 映射到一個向量。
    $$F(x, y, z) = P(x, y, z)i + Q(x, y, z)j + R(x, y, z)k$$
*   **物理直覺**：
    *   **速度場**：描述流體（水、風）在各點的速度。
    *   **力場**：描述物體在各點受到的力（如磁力、電力）。

---

## 一、向量場的視覺化
### 箭頭的地圖
1.  **繪圖方法**：在座標平面或空間選取樣本點，並畫出對應的向量 $F(x, y)$。
2.  **場線 (Flow Lines)**：想像粒子隨向量場運動的軌跡。
3.  **重力場範例**：
    $$F(r) = -\frac{mMG}{|r|^3}r$$
    這是一個**向心場**，所有向量都指向原點。

---

## 二、梯度場 (Gradient Fields)
### 從標量導出的向量場
若 $f(x, y, z)$ 是一個標量函數（位勢函數），其梯度 $\nabla f$ 就是一個向量場：
$$\nabla f = \frac{\partial f}{\partial x}i + \frac{\partial f}{\partial y}j + \frac{\partial f}{\partial z}k$$
*   **特性**：梯度場與等高面垂直。
*   **應用**：在地形圖上，梯度場指向最陡的山坡方向。

---

## 三、保守場 (Conservative Vector Fields)
### 具有「源頭」的場
如果一個向量場 $F$ 是某個標量函數 $f$ 的梯度場，即：
$$\mathbf{F = \nabla f}$$
則稱 $F$ 為**保守場**，而 $f$ 稱為 $F$ 的**位勢函數 (Potential Function)**。
*   **物理意義**：保守力（如重力、電力）所做的功與路徑無關。

---

## 💡 總結：16.1 的核心
*   **定義**：給每一點分配一個方向與長度。
*   **轉換**：學會區分標量函數（數值）與向量函數（向量場）。
*   **預覽**：下一節我們將學習如何「沿著曲線」累加這些場中的向量。

掌握了 16.1 節，您就建立了理解電磁、流體與現代物理動力學的數學地圖！
`;

// Preloaded Presentation: 微積分 16.2 — 線積分 (Line Integrals)
const DECK_CALCULUS_16_2_MD = String.raw`# 微積分 16.2：線積分
## 副標題：沿著路徑的累積——如何計算變力在彎曲軌跡上的總功
本簡報聚焦於 Stewart《Calculus》16.2 節，介紹對標量函數與向量場進行線積分的定義與運算。

---

## 16.2 導言：為什麼需要線積分？
### 突破直線積分的限制
*   **一維積分**：沿著 $x$ 軸（直線）累加。
*   **線積分**：沿著空間中的**曲線 $C$** 累加。
*   **應用**：
    *   計算一條非均勻鋼絲的總質量。
    *   計算變力 $F$ 沿著路徑 $C$ 對物體所做的功。

---

## 一、標量函數的線積分
### 計算質量與圍欄面積
設 $C$ 由 $r(t) = \langle x(t), y(t) \rangle$ 參數化，對 $f(x, y)$ 積分：
$$\int_C f(x, y) ds = \int_a^b f(x(t), y(t)) \mathbf{|r'(t)|} dt$$
*   **關鍵細節**：$ds = \sqrt{(x')^2 + (y')^2} dt$ 是弧長微分。
*   **意義**：若 $f=1$，結果即為曲線長度；若 $f$ 為密度，結果即為總質量。

---

## 二、向量場的線積分 (Vector Line Integrals)
### 物理中的「功」
給予向量場 $F$，沿著路徑 $C$ 的積分定義為：
$$\int_C F \cdot dr = \int_a^b F(r(t)) \cdot r'(t) dt$$
*   **幾何直覺**：計算場的向量在切線方向上的分量加總。
*   **計算步驟**：
    1.  參數化 $C$ 得到 $r(t)$。
    2.  求 $r'(t)$（切向量）。
    3.  將 $F$ 代入 $t$ 的表達式。
    4.  做內積並對 $t$ 積分。

---

## 三、路徑方向與分段積分
### 注意符號與結構
1.  **方向性**：對於向量場積分，方向改變會變號：$\int_{-C} F \cdot dr = -\int_C F \cdot dr$。
2.  **分段曲線**：若 $C = C_1 + C_2$，則總積分等於兩段積分之和。
3.  **對座標的線積分**：$\int_C P dx + Q dy$。
    *   這常與向量場積分互相轉換：$\int_C \langle P, Q \rangle \cdot dr$。

---

## 💡 總結：線積分的運算心法
*   **第一步必為參數化**：將 $x, y, z$ 全部轉化為 $t$ 的函數。
*   **區分 $ds$ 與 $dr$**：
    *   標量用速率 $|r'(t)|$。
    *   向量場用切向量 $r'(t)$ 並做內積。

掌握了 16.2 節，您就擁有了在動態環境中精確計算「作功」與「流量」的關鍵工具！
`;

// Preloaded Presentation: 微積分 16.3 — 線積分基本定理 (Fundamental Theorem for Line Integrals)
const DECK_CALCULUS_16_3_MD = String.raw`# 微積分 16.3：線積分基本定理
## 副標題：路徑無關的奧秘——當起點與終點決定了一切
本簡報聚焦於 Stewart《Calculus》16.3 節，探討保守向量場、路徑無關性以及能量守恆的數學表達。

---

## 16.3 導言：一場神奇的特例
### 條條大路通羅馬？
通常線積分與路徑有關，但在**保守場**中：
> **基本定理**：若 $F = \nabla f$，則
> $$\int_C \nabla f \cdot dr = f(r(b)) - f(r(a))$$
*   **意義**：積分值只取決於終點與起點，與中間走哪條路完全無關！

---

## 一、路徑無關性 (Path Independence)
### 閉合路徑的奇妙特性
若 $F$ 在區域 $D$ 內是路徑無關的，則下列命題等價：
1.  沿任何路徑的積分只看起點終點。
2.  沿任何**封閉路徑** $C$ 的線積分恆為 0：
    $$\oint_C F \cdot dr = 0$$
*   **物理對應**：在重力場中將物體繞一圈帶回原位，重力作功為 0。

---

## 二、如何判定保守場？
### 快速檢測方法
對於二維向量場 $F = P i + Q j$：
> **判準**：若 $F$ 在單連通區域上定義，且滿足
> $$\frac{\partial P}{\partial y} = \frac{\partial Q}{\partial x}$$
> 則 $F$ 必為保守場。
*   **三維情況**：需要利用 16.5 節的「旋度 (Curl)」來判定。

---

## 三、求位勢函數 $f$ (Finding Potential Functions)
### 反推「源頭」標量
已知 $F = \langle P, Q \rangle$，如何找出 $f$ 使得 $f_x = P, f_y = Q$？
1.  對 $P$ 關於 $x$ 積分：$f(x, y) = \int P dx + g(y)$。
2.  對上述結果關於 $y$ 求偏導。
3.  令 $\frac{\partial f}{\partial y} = Q$，解出待定函數 $g(y)$。
4.  最後合併得到完整位勢函數（加上常數 $C$）。

---

## 💡 總結：保守場的威力
*   **簡化計算**：不需要參數化複雜路徑，直接代入端點。
*   **物理洞察**：揭示了保守力場（重力、靜電場）的本質。
*   **注意**：定理成立的前提是區域必須是「單連通」的（沒有洞）。

掌握了 16.3 節，您就掌握了多變數微積分中「能量守恆」的嚴謹數學證明！
`;
