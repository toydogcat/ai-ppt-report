// Preloaded Presentation: 微積分 3.8 — 牛頓法 (Newton's Method)
const DECK_CALCULUS_3_8_MD = String.raw`# 微積分 3.8：牛頓法
## 副標題：利用切線無限逼近，求解複雜方程式的數值利器
本簡報聚焦於 Stewart《Calculus》3.8 節，介紹如何利用微分工具系統性地求解 $f(x) = 0$ 的近似根，並附帶 Python 實戰範例。

---

## 3.8 導言：為什麼需要牛頓法？
### 超越代數公式的極限
* **現狀**：二次方程有公式，三次四次極其複雜，五次以上則 **不存在** 一般根式解。
* **挑戰**：像 $\cos x = x$ 或 $x^5 - x - 1 = 0$ 這樣的方程式，無法用簡單代數求出精確值。
* **解決方案**：**牛頓法 (Newton-Raphson Method)**。這是一種迭代算法，能以極快的速度逼近真實解。

---

## 牛頓法的原理：切線逼近
### 幾何直覺
1. **起始點**：在根的附近選一個猜測值 $x_1$。
2. **作切線**：在點 $(x_1, f(x_1))$ 作出一條切線。
3. **求交點**：這條切線與 $x$ 軸的交點 $x_2$，通常會比 $x_1$ 更靠近真正的根。
4. **迭代**：重複此過程，$x_3, x_4, \dots$ 會迅速收斂到真實解。

---

## 牛頓法公式推導
### 從切線方程出發
點 $(x_n, f(x_n))$ 的切線方程為：
$$y - f(x_n) = f'(x_n)(x - x_n)$$
令 $y = 0$（求 $x$ 軸交點），並設交點為 $x_{n+1}$：
$$-f(x_n) = f'(x_n)(x_{n+1} - x_n)$$
解出 $x_{n+1}$，得到 **牛頓迭代公式**：
$$\boxed{x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}}$$

---

## Python 實戰：求解 $\cos x = x$
### 程式碼範例
\`\`\`python
import numpy as np

def newton_method(f, df, x0, tol=1e-7, max_iter=100):
    x = x0
    for i in range(max_iter):
        fx = f(x)
        dfx = df(x)
        if abs(dfx) < 1e-12: break # 避免除以零
        
        x_new = x - fx / dfx
        print(f"迭代 {i+1}: x = {x_new:.8f}")
        
        if abs(x_new - x) < tol: return x_new
        x = x_new
    return x

# 定義 f(x) = cos(x) - x 與其導數 f'(x) = -sin(x) - 1
result = newton_method(lambda x: np.cos(x) - x, 
                       lambda x: -np.sin(x) - 1, x0=1)
\`\`\`

---

## ⚠️ 牛頓法的潛在陷阱
### 什麼時候會失敗？
1. **導數為零**：若 $f'(x_n) \approx 0$，公式中的分母會消失，導致計算崩潰或點噴向無窮遠。
2. **初始值選得太差**：若起始點不在根的附近，可能會收斂到錯誤的根，或者陷入循環。
3. **震盪行為**：某些函數會導致 $x_n$ 在兩個值之間跳動，永不收斂。

*結論：在使用牛頓法前，先畫個草圖確定 $x_1$ 的位置是非常明智的。*

---

## 💡 總結
* **本質**：將「曲線問題」局部轉化為「直線問題」。
* **效率**：具備「平方收斂」特性（有效位數在每一輪迭代中幾乎翻倍）。
* **地位**：它是現代工程計算、優化算法與計算機圖圖形的底層基石。

數學的嚴謹與計算的效率，在牛頓法中得到了完美的統一。
`;
