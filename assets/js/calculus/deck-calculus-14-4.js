// Preloaded Presentation: 微積分 14.4 — 切平面與線性近似 (Tangent Planes and Linear Approximations)
const DECK_CALCULUS_14_4_MD = String.raw`# 微積分 14.4：切平面與線性近似
## 副標題：曲面的局部平滑化——用簡單的平面理解複雜的形狀
本簡報聚焦於 Stewart《Calculus》14.4 節，介紹切平面的推導、線性逼近函數值的方法，以及全微分在誤差估計中的應用。

---

## 一、切平面方程式 (Tangent Planes)
### 捕捉某一點的即時平面
若曲面方程為 $z = f(x, y)$，且 $f$ 在點 $P(x_0, y_0, z_0)$ 可微，其切平面方程式為：
$$z - z_0 = f_x(x_0, y_0)(x - x_0) + f_y(x_0, y_0)(y - y_0)$$
*   **幾何連結**：這就像是一元微積分中切線方程式 $y - y_0 = f'(x_0)(x - x_0)$ 的 2D 擴展版本。
*   **物理直覺**：在極小的範圍內，任何平滑曲面看起來都像是一個平面。

---

## 二、線性近似 (Linearization)
### 簡化複雜的計算
我們將切平面對應的函數稱為 $f$ 在 $(x_0, y_0)$ 的**線性逼近 (Linearization)** $L(x, y)$：
$$f(x, y) \approx L(x, y) = f(x_0, y_0) + f_x(x_0, y_0)(x - x_0) + f_y(x_0, y_0)(y - y_0)$$
*   **應用場景**：當我們需要快速估算某個複雜公式（如 $\sqrt{1.01^2 + 2.99^2}$）的近似值時。
*   **核心思維**：用一次多項式取代超越函數。

---

## 三、可微性 (Differentiability)
### 超越「偏導數存在」的要求
在一維中，導數存在即代表可微；但在高維度中，即使 $f_x, f_y$ 存在，函數也可能不可微（可能很「尖銳」）。
*   **定義**：可微意味著線性近似 $L(x, y)$ 是一個「夠好」的逼近。
*   **充分條件**：若偏導數 $f_x, f_y$ 在點的附近**存在且連續**，則 $f$ 在該點必可微。

---

## 四、全微分 (Total Differential)
### 追蹤變動的細微影響
對於 $z = f(x, y)$，定義全微分 $dz$ 為：
$$dz = f_x(x, y) dx + f_y(x, y) dy = \frac{\partial z}{\partial x} dx + \frac{\partial z}{\partial y} dy$$
*   **$dz$ vs. $\Delta z$**：
    *   $\Delta z$：函數值的「真實」改变量。
    *   $dz$：利用切平面預測的「線性」改变量。
*   **用途**：常用於**誤差分析**。例如：長度量測有 $1\%$ 誤差，面積會產生多少誤差？

---

## 五、實戰範例：誤差估計
### 測量一個圓柱體的體積
*   **已知**：半徑 $r$, 高度 $h$。體積 $V = \pi r^2 h$。
*   **問題**：測量誤差為 $dr, dh$，體積最大誤差 $dV$ 是多少？
*   **計算**：
    $$dV = \frac{\partial V}{\partial r} dr + \frac{\partial V}{\partial h} dh = (2\pi rh) dr + (\pi r^2) dh$$
*   這讓我們能精確量化工程測量中的風險。

---

## 💡 總結：化繁為簡的威力
*   **切平面**：給曲面穿上一件「局部平貼」的衣服。
*   **線性近似**：將複雜運算轉化為加減乘除。
*   **微分 $dz$**：捕捉各個方向變動對總結果的貢獻。

掌握了 14.4 節，您就學會了如何將複雜的多維世界「局部平坦化」，以便進行更直觀的分析！
`;
